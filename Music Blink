/*
  Name: Marcus Kim
  Teacher Name: Mr. So
  Date: April 10, 2025
  Title:Music Blink
  Description: using a buzzer/speaker to play a song and have LEDs dance to it
*/

/////////// tone section

#include <Tone.h> //include ToneLibrary
const byte tonePin1 = 12; // set first buzzer pin
const byte tonePin2 = 13; // set second buzzer pin

Tone buzzerPin1;//set the variables to be toned
Tone buzzerPin2; // set the variables to be toned

/////////// led pin assigning section

int greenLed1 = 11; //set pin for 1st green led
int greenLed2 = 10; // set pin for 2nd green led
int greenLed3 = 9; // set pin for 3rd green led
int greenLed4 = 8; // set pin for 4th green led
int redLed1 = 7; // set pin for 1st red led
int redLed2 = 6; // set pin for 2nd red led
int redLed3 = 5; // set pin for 3rd red led
int redLed4 = 4; // set pin for 4th red led

/////////// note duration section

//140 bpm section

int eighthNote = 200;// set amount of time the tone will be delayed for an eighth note at 140 bpm
int quarterNote = 400;// set amount of time the tone will be delayed for a quarter note in ms at 140 bpm
int halfNote = 800; // set amount of time the tone will be delayed for a half note in ms at 140 bpm
int dottedHalf = 1200;// set amount of time the tone will be delayed for a dotted half note in ms at 140 bpm
int wholeNote = 1600; // set amount of time the tone will be delayed for a whole note in ms at 140 bpm
int tripletNote = quarterNote / 3; // set amount of time the tone will be delayed for a triplet quarter note in ms at 140 bpm
int delayCut = 5; // set amount of time tone will be delayed for cutting off a note

//170 bpm section

int fastEighthNote = 176;// set amount of time the tone will be delayed for an eighth note in ms at 170 bpm
int fastQuarterNote = 353;// set amount of time the tone will be delayed for a quarter note in ms at 170 bpm
int fastHalfNote = 706;// set amount of time the tone will be delayed for a half note in ms at 170 bpm
int fastDottedHalf = 1059;// set amount of time the tone will be delayed for a dotted half note in ms at 170 bpm
int fastWholeNote = 1412;// set amount of time the tone will be delayed for a whole note in ms at 170 bpm
int fastTripletNote = fastQuarterNote / 3; // set amount of time the tone will be delayed for a triplet quarter note in ms at 170 bpm

/////////// note declaration section
// S = sharp (e.g., CS4 = C sharp 4th octave)
// frequency table = https://github.com/RodrigoDornelles/arduino-tone-pitch

int noteA2 = 110; // declare variable for note frequency
int noteC3 = 130;  // declare variable for note frequency
int noteCS3 = 138; // declare variable for note frequency
int noteD3 = 146;  // declare variable for note frequency
int noteDS3 = 155; // declare variable for note frequency
int noteE3 = 164;  // declare variable for note frequency
int noteF3 = 174; // declare variable for note frequency
int noteFS3 = 185; //declare variable for note frequency
int noteG3 = 196; //declare variable for note frequency
int noteGS3 = 207; //declare variable for note frequency
int noteA3 = 220; //declare variable for note frequency
int noteAS3 = 233; //declare variable for note frequency
int noteB3 = 246; //declare variable for note frequency
int noteC4 = 261; //declare variable for note frequency
int noteCS4 = 277; //declare variable for note frequency
int noteD4 = 293; //declare variable for note frequency
int noteDS4 = 311; //declare variable for note frequency
int noteE4 = 329; //declare variable for note frequency
int noteF4 = 349; //declare variable for note frequency
int noteFS4 = 369; //declare variable for note frequency
int noteG4 = 392; //declare variable for note frequency
int noteGS4 = 415; //declare variable for note frequency
int noteA4 = 440; //declare variable for note frequency
int noteAS4 = 466; //declare variable for note frequency
int noteB4 = 493; //declare variable for note frequency
int noteC5 = 523; //declare variable for note frequency
int noteCS5 = 554; //declare variable for note frequency
int noteD5 = 587; //declare variable for note frequency
int noteDS5 = 622; //declare variable for note frequency
int noteE5 = 659; //declare variable for note frequency
int noteF5 = 698; //declare variable for note frequency
int noteFS5 = 739; //declare variable for note frequency
int noteG5 = 783; //declare variable for note frequency
int noteGS5 = 830; //declare variable for note frequency
int noteA5 = 880; //declare variable for note frequency
int noteAS5 = 932; //declare variable for note frequency
int noteB5 = 987;//declare variable for note frequency
int noteC6 = 1046; //declare variable for note frequency

void setup() {

  pinMode(greenLed1, OUTPUT); // set led as output
  pinMode(greenLed2, OUTPUT); // set led as output
  pinMode(greenLed3, OUTPUT); // set led as output
  pinMode(greenLed4, OUTPUT); // set led as output
  pinMode(redLed1, OUTPUT); // set led as output
  pinMode(redLed2, OUTPUT); // set led as output
  pinMode(redLed3, OUTPUT); // set led as output
  pinMode(redLed4, OUTPUT); // set led as output


  buzzerPin1.begin(tonePin1); //begins the tones into respective pins
  buzzerPin2.begin(tonePin2); // begins the tones into respective pins

  // sheet music = https://musescore.com/user/74946346/scores/21682483

  /////////// measure 1

  digitalWrite(greenLed1, HIGH); // turn on LED
  buzzerPin1.play(noteF3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteA3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteC4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteE4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteA4); // play a frequency at the pin
  delay(halfNote); // hold the tone for the length of a half note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED

  /////////// measure 2

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteF3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteA3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteC4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteG4); // play a frequency at the pin
  delay(halfNote); // hold the tone for the length of a half note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED

  /////////// measure 3

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteE3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteG3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteB3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteG4); // play a frequency at the pin
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(quarterNote); // hold the tone for the length of a quarter note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(quarterNote); // hold the tone for the length of a quarter note in ms
  buzzerPin1.stop();// stop the tone from the pin
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED


  /////////// measure 4

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteCS4); // play a frequency at the pin
  buzzerPin1.play(noteA3); // play a frequency at the pin
  delay(quarterNote); // hold the tone for the length of a quarter note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteG3); // play a frequency at the pin
  delay(quarterNote); // hold the tone for the length of a quarter note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteF3); // play a frequency at the pin
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(quarterNote / 2); // hold the tone for the length of a quarter note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin2.play(noteF4); //play a frequency at the pin
  delay(eighthNote / 1.5); // hold the tone for the length of a eighth note in ms
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteE3); //play a frequency at the pin
  delay(eighthNote);// hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteE4); //play a frequency at the pin
  delay(eighthNote);// hold the tone for the length of a triplet note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED

  /////////// measure 5

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of a eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of a eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteA3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteC4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  delay(halfNote); // hold the tone for the length of a half note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 6

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteD3); // play a frequency at the pin
  buzzerPin1.play(noteD4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteGS3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  delay(quarterNote); // hold the tone for the length of a quarter note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin1.play(noteC4); // play a frequency at the pin
  delay(quarterNote); // hold the tone for the length of a quarter note in ms
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED



  /////////// measure 7

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC3); // play a frequency at the pin
  buzzerPin1.play(noteB3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED


  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED


  /////////// measure 8

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteG4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteB4); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC5); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE5); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteG5); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteB5); // play a frequency at the pin
  delay(eighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  /////////// measure 9

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC6); // play a frequency at the pin
  delay(wholeNote); // hold the tone for the length of a whole note in ms
  buzzerPin2.stop();// stop the tone from the pin
  delay(delayCut); // hold the tone to cut off the end of the note in ms
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED


  /////////// measure 10

  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC6); // play a frequency at the pin
  delay(halfNote); // hold the tone for the length of a half note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB5); // play a frequency at the pin
  delay(halfNote); // hold the tone for the length of a half note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED

  /////////// measure 11
  /////////// * tempo change from 140 to 170 bpm, change to faster notes

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin1.play(noteA5); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 12

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 13

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 14

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteA4); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteC5); // play a frequency at the pin
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED


  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED


  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteB4); // play a frequency at the pin
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 15

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteG4); // play a frequency at the pin
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 16

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 17


  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteE4); // play a frequency at the pin
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 18

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteE4); // play a frequency at the pin
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteE4); // play a frequency at the pin
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteG4); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 19

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED


  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 20


  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED


  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 21

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED


  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 22

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED


  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteC4); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 23

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin1.play(noteB3); // play a frequency at the pin
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.stop();// stop the tone from the pin
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 24

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 25
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteB4); // play a frequency at the pin
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteC5); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteD5); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 26

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteE5); // play a frequency at the pin
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteB5); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteC6); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 27

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteA5); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 28

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 29

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteA5); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 30

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteA5); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteC6); // play a frequency at the pin
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteE4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin1.play(noteB5); // play a frequency at the pin
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 31

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin1.play(noteG5); // play a frequency at the pin
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 32

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 33

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteE5); // play a frequency at the pin
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms



  /////////// measure 34

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteE5); // play a frequency at the pin
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD5); // play a frequency at the pin
  buzzerPin2.play(noteD4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin1.play(noteG5); // play a frequency at the pin
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin1.play(noteE5); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 35

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin1.play(noteD5); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 36

  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed4, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 37

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD5); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed4, HIGH);// turn on LED
  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 38

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD5); // play a frequency at the pin
  buzzerPin2.play(noteD3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin1.play(noteC5); // play a frequency at the pin
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD5); // play a frequency at the pin
  buzzerPin2.play(noteA3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteC5); // play a frequency at the pin
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 39

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed1, HIGH);// turn on LED
  digitalWrite(redLed2, HIGH);// turn on LED
  digitalWrite(redLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteB4); // play a frequency at the pin
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed2, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 40

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms


  /////////// measure 41
  /////////// * slow down to create rallentando effect so multiply by 1.1, .2, .3, .4, …

  digitalWrite(greenLed1, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteB3); // play a frequency at the pin
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote * 1.1); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed1, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote * 1.2); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteC4); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote * 1.3); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote * 1.4); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote * 1.5); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote * 1.6); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 42

  digitalWrite(greenLed4, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteE4); // play a frequency at the pin
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote * 1.7); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed4, LOW);// turn off LED

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote * 1.8); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed3, LOW);// turn off LED

  digitalWrite(greenLed3, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteD4); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote * 1.9); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED

  digitalWrite(redLed2, HIGH);// turn on LED
  buzzerPin2.play(noteB3); // play a frequency at the pin
  delay(fastEighthNote * 2); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(redLed2, LOW);// turn off LED

  digitalWrite(greenLed2, HIGH);// turn on LED
  digitalWrite(redLed4, HIGH);// turn on LED
  buzzerPin1.play(noteB3); // play a frequency at the pin
  buzzerPin2.play(noteG3); // play a frequency at the pin
  delay(fastEighthNote * 2.1); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed4, LOW);// turn off LED
  digitalWrite(greenLed2, LOW);// turn off LED

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteE3); // play a frequency at the pin
  delay(fastEighthNote * 2.2); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin
  digitalWrite(redLed1, LOW);// turn off LED
  delay(delayCut); // hold the tone to cut off the end of the note in ms

  /////////// measure 43
  /////////// * divide by 2 to create glissando effect

  digitalWrite(greenLed1, HIGH);// turn on LED
  buzzerPin2.play(noteA2); // play a frequency at the pin
  delay(fastEighthNote / 2); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin

  digitalWrite(greenLed3, HIGH);// turn on LED
  buzzerPin2.play(noteC3); // play a frequency at the pin
  delay(fastEighthNote / 2); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin

  digitalWrite(redLed1, HIGH);// turn on LED
  buzzerPin2.play(noteF3); // play a frequency at the pin
  delay(fastEighthNote / 2); // hold the tone for the length of an eighth note in ms
  buzzerPin2.stop();// stop the tone from the pin

  digitalWrite(redLed3, HIGH);// turn on LED
  buzzerPin1.play(noteA3); // play a frequency at the pin
  buzzerPin2.play(noteC4); // play a frequency at the pin
  delay(fastDottedHalf * 2.3); // hold the tone for the length of a dotted half note in ms
  buzzerPin2.stop();// stop the tone from the pin
  buzzerPin1.stop();// stop the tone from the pin
  digitalWrite(greenLed1, LOW);// turn off LED
  digitalWrite(greenLed3, LOW);// turn off LED
  digitalWrite(redLed1, LOW);// turn off LED
  digitalWrite(redLed3, LOW);// turn off LED

}



void loop() {


}


